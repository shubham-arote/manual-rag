# Railway upload ignore rules.
# This file overrides .gitignore for `railway up` uploads.
# We intentionally include lancedb_index/ and output/ so they
# get baked into the Docker image at build time.

# Never upload
.venv/
venv/
.git/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.mypy_cache/
.ruff_cache/
tests/evaluation/results/
.env
.env.*
